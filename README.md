install via:

```
makepkg -si

In the selection menu, select 1. chan_mobile and 2. Drivers > chan_sip.

And after install you might need to run `asterisk` to get the service up.

asterisk -rvvvv
# this should work now...
```
